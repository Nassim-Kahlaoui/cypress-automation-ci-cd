trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self

  # Cache Node modules (optional)
  - task: Cache@2
    inputs:
      key: 'npm | "$(Agent.OS)" | package-lock.json'
      restoreKeys: |
        npm | "$(Agent.OS)"
      path: 'node_modules'

  # Install Node
  - task: NodeTool@0
    inputs:
      versionSpec: '20.x'
    displayName: 'Install Node.js'

  # Install dependencies + Cypress
  - script: |
      npm install
      npx cypress install
    displayName: 'Install Cypress & deps'

  # Run Cypress tests
  - script: |
      npx cypress run
    displayName: 'Run Cypress Tests'

  # Generate Mochawesome Report
  - script: |
      node generate-report.js
      node write-results.js
    displayName: 'Generate Mochawesome Report'

  # Publish mochawesome
  - task: PublishBuildArtifacts@1
    displayName: 'Publish mochawesome'
    inputs:
      PathtoPublish: 'cypress/reports/mochawesome'
      ArtifactName: 'mochawesome'
      publishLocation: 'Container'
    continueOnError: true

  # Publish videos
  - task: PublishBuildArtifacts@1
    displayName: 'Publish videos'
    inputs:
      PathtoPublish: 'cypress/videos'
      ArtifactName: 'videos'
      publishLocation: 'Container'
    continueOnError: true

  # Publish screenshots
  - task: PublishBuildArtifacts@1
    displayName: 'Publish screenshots'
    inputs:
      PathtoPublish: 'cypress/screenshots'
      ArtifactName: 'screenshots'
      publishLocation: 'Container'
    continueOnError: true

  # Publish HTML report
  - task: PublishBuildArtifacts@1
    displayName: 'Publish HTML report'
    inputs:
      PathtoPublish: 'cypress/reports/mochawesome/html'
      ArtifactName: 'html-report'
      publishLocation: 'Container'
    continueOnError: true
